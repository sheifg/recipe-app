import Home from "./Home";
import About from "./About";
import Login from "./Login";
import Details from "./Details";

export { Home, About, Login, Details };

// index.jsx is created to import these components in one line. It is optional

// When the components are imported in the file, it is not necessary to specify that is the index, react can recognize with index.jsx
